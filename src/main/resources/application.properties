# Application name
spring.application.name=bank-conta
server.port=8080

# Altera a estrutura da tabela caso a entidade tenha mudanças
spring.jpa.hibernate.ddl-auto=update

# Dados conexão
spring.datasource.url=jdbc:postgresql://172.17.0.3:5432/gomes_bank
spring.datasource.username=postgres
spring.datasource.password=SisPort@Server
spring.datasource.driver-class-name=org.postgresql.Driver

# Mostrar sql
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Token
jwt.secret=GomesBank2023
jwt.expiration=864000

# Eureka Client
eureka.client.service-url.default-zone=http://172.17.0.2:8761/eureka
eureka.instance.instance-id=${spring.application.name}:${random.int}
eureka.client.fetch-registry=true
eureka.client.register-with-eureka=true
management.endpoints.web.exposure.include=*
